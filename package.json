{
  "name": "resume-site",
  "version": "1.0.0",
  "scripts": {
    "build": "pug views/ -o docs/ --pretty",
    "watch": "pug -w views/ -o docs/ --pretty"
  },
  "devDependencies": {
    "pug-cli": "^1.0.0"
  }
}
